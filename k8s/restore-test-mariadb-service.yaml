apiVersion: v1
kind: Service
metadata:
  name: mariadb-service
  namespace: ecommerce-restore-test
spec:
  type: ClusterIP
  selector:
    app: mariadb
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
