apiVersion: v1
kind: Service
metadata:
  name: frontend-ecommerce-service
  namespace: ecommerce

spec:
  type: ClusterIP

  selector:
    app: frontend-ecommerce

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
