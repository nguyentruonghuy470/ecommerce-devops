apiVersion: v1
kind: Service
metadata:
  name: backend-ecommerce-service
  namespace: ecommerce
spec:
  type: ClusterIP

  selector:
    app: backend-ecommerce

  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
